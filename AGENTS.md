# AGENTS.md — تعليمات الوكيل لمشروع ماركت طيبه

## الإشعار عبر تلغرام (إلزامي في كل جلسة عمل)
سكربت الإرسال: `node scripts/telegram-notify.mjs` — send-only عبر sendMessage،
بدون انتظار أي رد من تلغرام، وفشل الشبكة يُتجاهل بصمت (خروج 0 دائماً).

- **الحالة أ — بعد اكتمال أي مهمة بنجاح:**
  `node scripts/telegram-notify.mjs done "ملخص 3-5 أسطر بالعربي: الملفات الرئيسية المعدلة + نتيجة tsc/vite"`
- **الحالة ب — عند التوقف بانتظار موافقة المستخدم** (أمر حساس، حذف ملفات، تعديل غير آمن، أو أي سؤال يوقف العمل):
  `node scripts/telegram-notify.mjs waiting <label-فريد-للنقطة> "على أي إذن بالتحديد"`
  يُرسل مرة واحدة فقط لنفس النقطة (منع تكرار مدمج عبر ملف حالة خارج المستودع).
- المفاتيح في `.env.local` بجذر المشروع: `TELEGRAM_BOT_TOKEN`, `TELEGRAM_CHAT_ID` —
  لا تُكتب في أي كود ولا تُلصق في المحادثة، والملف متجاهل من git.
- **آلية الموافقة نفسها تبقى في الشات العادي كما هي** — تلغرام تنبيه فقط ولا يُستقبل منه شيء.
- لا يُستدعى أي endpoint آخر من Telegram API إطلاقاً.

## فحص المشروع
- typecheck: `npm run lint` (tsc --noEmit)
- build: `npm run build`
